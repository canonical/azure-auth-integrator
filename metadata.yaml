name: azure-auth-integrator

summary: Charm for providing access to Azure service principal credentials.

description: |
  Charm for providing access to Azure service principal credentials

provides:
  azure-service-principal-credentials:
    interface: azure_service_principal
    limit: 1
